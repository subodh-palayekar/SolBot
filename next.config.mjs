/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'upload.wikimedia.org',
      'cdn4.iconfinder.com',
      'api.microlink.io',
    ],
  },
};

export default nextConfig;
